# Spinning Wheel

A simple browser-based spinning wheel that picks a random name from a list.

## Usage

Open `index.html` in your browser and click **Spin**. Edit the `names` array
at the top of `script.js` to use your own list.
